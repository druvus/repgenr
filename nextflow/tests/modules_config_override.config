// Test-only: prove a site config can retune one process through ext.args.
// debug makes the stub's stdout visible to nf-test's process.stdout.
process {
    withName: 'PHYLO' {
        ext.args = '--treebuilder faketree-from-config'
        debug = true
    }
}
