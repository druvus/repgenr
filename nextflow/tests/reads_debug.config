// Test-only: make the reads-mode stubs' stdout visible to nf-test's
// process.stdout so the ext.* values they echo can be asserted.
process {
    withName: 'READS_ASSEMBLE|GENOME_QC|READS_GATHER|READS_SELECT' {
        debug = true
    }
}
